<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">
        <record id="mail.mail_channel_rule" model="ir.rule">
            <field name="domain_force">['|', '|',
                ('public', '=', 'public'),
                '&amp;', ('public', 'in', ('private', 'department')), ('channel_partner_ids', 'in', [user.partner_id.id]),
                '&amp;', ('public', '=', 'groups'), ('group_public_id', 'in', [g.id for g in user.groups_id])]</field>
        </record>
    </data>
</odoo>
